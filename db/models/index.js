const bannerModel = require('./bannerModel');
const musicModel = require('./musicModel');
const passwordModel = require('./passwordModel');
const playlistModel = require('./playlistModel');
const tokenModels = require('./tokenModels');
const userModels = require('./userModel');

module.exports = {
    userModels,
    tokenModels,
    musicModel,
    passwordModel,
    playlistModel,
    bannerModel
};
